{
  "name": "foliod",
  "version": "1.0.0",
  "private": true,
  "description": "Self-curating GenAI portfolio — a local Ollama agent watches GitHub and Medium, drafts new entries, and opens a PR to keep the site current.",
  "type": "commonjs",
  "scripts": {
    "generate": "node scripts/generate-config.js",
    "poll": "node agent/poll.js",
    "poll:dry": "node agent/poll.js --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
